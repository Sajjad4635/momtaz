import 'package:flutter/material.dart';

class feauter extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('این بخش در آینده اضافه خواهد شد'),
    );
  }
}
